# Outputs captured from `analyze_math_results.ipynb` before converting to a plain .py script.

# Preserved for reference — these are real recorded execution results, not regenerated.


## Cell 0 (id: cell-0)

```
Hoàn thành tải 7500 câu.
```

## Cell 2 (id: cell-2)

```
<Figure size 1200x600 with 1 Axes>
```

## Cell 4 (id: cell-4)

```
<Figure size 1200x600 with 1 Axes>
```

## Cell 6 (id: cell-6)

```
C:\Users\tranh\AppData\Local\Temp\ipykernel_15384\816534418.py:14: FutureWarning: DataFrame.groupby with axis=1 is deprecated. Do `frame.T.groupby(...)` without axis instead.
  pivot_pct = pivot_table.div(pivot_table.groupby(level=0, axis=1).sum(), axis=1) * 100

<Figure size 1600x800 with 2 Axes>
```
